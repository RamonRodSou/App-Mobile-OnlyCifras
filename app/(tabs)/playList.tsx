import { ScrollView, Text } from 'react-native';

export default function PlayList() {
  return (
    <ScrollView>
      <Text className='text-title text-[2rem]'>Playlist</Text>
    </ScrollView>
  );
}